module MDocker
  class Project

    def initialize(config, repository, lock_path)
      @config = config
      @repository = repository
      @lock_path = lock_path
    end

    def build_hash

    end

  end
end